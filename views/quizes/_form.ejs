<p><label for="preg" class="rem">Pregunta:</label>
<input type="text" id="preg" name="quiz[pregunta]" placeholder="Introduzca su pregunta" value=
	<% if (quiz.pregunta == 'Pregunta') { %>
		""
	<% } else { %>
		"<%= quiz.pregunta %>"
	<% } %>
/></p>
<p><label for="resp" class="rem">Respuesta:</label>
<input type="text" id="resp" name="quiz[respuesta]" placeholder="Introduzca su respuesta" value=
	<% if (quiz.respuesta == 'Respuesta') { %>
		""
	<% } else { %>
		"<%= quiz.respuesta %>"
	<% } %>
/></p>
<p><label for="tema" class="rem">Tema:</label>
<select id="tema" name="quiz[tema]">
	<option value=""
	<% if (quiz.tema == '') { %>
		selected
	<% } %>
	>Seleccione un tema</option>
	<option value="humanidades"
	<% if (quiz.tema == 'humanidades') { %>
		selected
	<% } %>
	>Humanidades</option>
	<option value="ocio"
	<% if (quiz.tema == 'ocio') { %>
		selected
	<% } %>
	>Ocio</option>
	<option value="ciencia"
	<% if (quiz.tema == 'ciencia') { %>
		selected
	<% } %>
	>Ciencia</option>
	<option value="tecnologia"
	<% if (quiz.tema == 'tecnologia') { %>
		selected
	<% } %>
	>Tecnología</option>
	<option value="otro"
	<% if (quiz.tema == 'otro') { %>
		selected
	<% } %>
	>Otro</option>
</select>
</p>
<a href="/quizes"><button type="button">Cancelar</button></a>
<input type="submit" value="Guardar">